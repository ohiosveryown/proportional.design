{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "foto",
  "title": "Foto",
  "description": "Upload a photo to your Foto gallery with caption and tags",
  "icon": "command-icon.png",
  "author": "ohiosveryown",
  "categories": ["Productivity"],
  "license": "MIT",
  "commands": [
    {
      "name": "upload-photo",
      "title": "Upload Photo",
      "subtitle": "Foto",
      "description": "Upload an image to Foto with a caption and optional tags",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "apiBaseUrl",
      "title": "API Base URL",
      "description": "Base URL of the Foto app",
      "type": "textfield",
      "default": "https://fotos.proportional.design",
      "required": true
    },
    {
      "name": "sharedSecret",
      "title": "Shared Secret",
      "description": "Bearer token. Must match GALLERY_SECRET on the server.",
      "type": "password",
      "required": true
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.85.0",
    "@raycast/utils": "^1.18.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "^20.10.0",
    "@types/react": "19.0.10",
    "eslint": "^8.57.0",
    "react": "19.0.0",
    "typescript": "^5.4.0"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix"
  }
}
